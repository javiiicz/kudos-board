import "../styles/AddCardButton.css"

const CreateButton = () => {
    return (
        <button className="add-button">
            <p>+ Add a new Card</p>
        </button>
    )
}

export default CreateButton;